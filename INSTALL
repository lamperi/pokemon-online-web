You need Python 2.7 to run this.
Install dependencies using:
$ pip install twisted
$ pip install txsockjs

To get the PO protocol installed:
$ git submodule init
$ git submodule update

To run the web server, you currently need to copy or link
Themes and db from Pokemon Online installation to
static/Themes and static/db
Hopefully this will change at some point.

To run the service:
twistd -n -y powebservice.tac
